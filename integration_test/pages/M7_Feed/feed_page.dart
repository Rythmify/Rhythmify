import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import '../base_page.dart';
import '../../selectors/selectors.dart';

class FeedPage extends BasePage {
  FeedPage(WidgetTester tester) : super(tester);

  // Future<void> tapFeedButton() async => await tapByKey(feedNavButton);
  // Future<void> tapDiscoverButton() async => await tapByKey(DiscoverButton);
  // Future<void> tapFollowingButton() async => await tapByKey(followButton);
  // Future<void> taplikeButton() async => await tapByKey(likeButton);
  // Future<void> tapCommentButton() async => await tapByKey(commentButton);
  // Future<void> DragTrackCard() async => await tester.drag(find.byKey(trackCard), const Offset(0, -300));
  // bool PlayTrack() => isVisible (playerTrackInfoBoxDetails);
  // Future<void> tapDragtButton() async => await tapByKey(DragButton);
  // Future<void> scrollDown() async {
  //   const Offset(0, -100);
  //   maxIteration: 50;
  //   await tester.pump(const Duration(milliseconds: 500));
  // }
  // Future<void> scrollUp() async {
  //   const Offset(0, 100);
  //   maxIteration: 50;
  //   await tester.pump(const Duration(milliseconds: 500));
  // }

}
